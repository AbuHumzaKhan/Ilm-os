FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1

RUN pip install --no-cache-dir pandas==2.2.3

RUN useradd --create-home --uid 10001 runner
WORKDIR /workspace
USER runner

CMD ["python", "-I", "/workspace/main.py"]
